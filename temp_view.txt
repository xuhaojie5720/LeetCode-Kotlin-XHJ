```kotlin
// 位运算：�?26 �?bitmask 记录是否出现过（假设均为小写字母 a..z�?class Solution {
    fun isUnique(astr: String): Boolean {
        if (astr.length > 26) return false // 鸽巢原理：字符数�?26 必有重复

        var mask = 0
        for (ch in astr) {
            val bit = 1 shl (ch - 'a')
            if ((mask and bit) != 0) return false // 已出�?            mask = mask or bit
        }
        return true
    }
}
```

说明
- 思路：位掩码的第 k 位表示字�?`('a' + k)` 是否出现，重复则该位已为 1�?- 复杂度：时间 O(n)，空�?O(1)�?- Kotlin 特性：`shl/and/or` 位运算；`for (ch in astr)` 直接遍历字符；`if` 作为表达式早返回�?- 备注：若字符集为扩展 ASCII/Unicode，应改为布尔数组�?`BitSet`，此处按题意放在位运算类别，�?26 位掩码实现�?
